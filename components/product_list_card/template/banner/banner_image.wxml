<block>
    <view class="pro-title">{{product.name}}</view>
    <view class="pro-subtitle">{{product.description}}</view>
    <view class="pro-banner">
        <image class="pro-banner-image" lazy-load="{{true}}" mode="scaleToFill" src="{{product.images[0]}}"/>
    </view>
</block>